{
  "name": "claude-warden",
  "version": "0.1.0",
  "description": "An independent watchdog that audits every Claude Code tool call for malicious activity and fatal mistakes, before it runs.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "warden": "./dist/cli.js"
  },
  "main": "./dist/pipeline.js",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "pretest": "tsc -p tsconfig.test.json",
    "test": "node --test dist-test/test/*.test.js",
    "test:smoke": "tsc -p tsconfig.test.json && node --test dist-test/test/smoke.test.js",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "claude",
    "claude-code",
    "security",
    "hook",
    "agent",
    "guardrail",
    "prompt-injection"
  ],
  "dependencies": {
    "@anthropic-ai/sdk": "^0.68.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
